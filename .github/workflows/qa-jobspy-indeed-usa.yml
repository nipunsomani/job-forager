name: QA JobSpy Indeed USA

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -e .
      - name: Indeed simple query + USA location
        env:
          PYTHONPATH: src
        run: |
          python -m jobforager.cli search \
            --sources indeed \
            --title-keywords "security engineer" \
            --location "New York, USA" \
            --last 5h \
            --output-json results.json
      - uses: actions/upload-artifact@v4
        with:
          name: qa-indeed-usa
          path: results.json
          retention-days: 1
